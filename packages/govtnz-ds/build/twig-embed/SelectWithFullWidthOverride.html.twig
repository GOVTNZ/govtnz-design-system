<div
class="g-selectWithFullWidthOverride-form-group"
>
  
  
<label
class="g-selectWithFullWidthOverride-label"
for="{{ select1 }}"
>
  
    Label text goes here
  
</label>
  
  
<select
class="g-selectWithFullWidthOverride-select g-!-width-full"
id="{{ select1 }}"
name="{{ name }}"
multiple="{{ if multiple }}multiple{{ endif }}"
>
  
    
<option
value="1"
selected="{{ if selected }}selected{{ endif }}"
>
  Govt.NZ frontend option 1
</option>
  
    
<option
selected="{{ if selected2 }}selected2{{ endif }}"
value="2"
>
  Govt.NZ frontend option 2
</option>
  
    
<option
disabled=""
value="3"
selected="{{ if selected3 }}selected3{{ endif }}"
>
  Govt.NZ frontend option 3
</option>
  
  
</select>
  

</div>