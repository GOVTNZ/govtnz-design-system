<div
>
  
        
<label
class="g-selectBlock-label"
for="{{ selectId }}"
>
  
          
  {{{label}}}
  
        
</label>
  
        
<select
class="g-selectBlock-select{{ if hasError }} g-selectBlock-select--error{{ endif }}"
id="{{ selectId }}"
name="{{ name }}"
multiple="{{ if multiple }}multiple{{ endif }}"
>
  {{{children}}}
</select>
  
      
</div>