import React from "react";
declare type Props = {
    spacing?: boolean;
    marginBottom0?: boolean;
    noBullet?: boolean;
    children?: React.ReactNode;
};
declare const Ul: ({ spacing, marginBottom0, noBullet, children }: Props) => JSX.Element;
export default Ul;
