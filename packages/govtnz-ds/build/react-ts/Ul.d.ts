import React from "react";
declare type Props = {
    bulleted?: boolean;
    spacing?: boolean;
    children?: React.ReactNode;
};
declare const Ul: ({ bulleted, spacing, children }: Props) => JSX.Element;
export default Ul;
