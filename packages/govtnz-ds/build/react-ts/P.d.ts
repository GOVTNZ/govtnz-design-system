import React from "react";
declare type Props = {
    styleSize: "large" | "medium" | "small" | "x-small";
    marginBottom0?: boolean;
    children?: React.ReactNode;
};
declare const P: ({ styleSize, marginBottom0, children }: Props) => JSX.Element;
export default P;
