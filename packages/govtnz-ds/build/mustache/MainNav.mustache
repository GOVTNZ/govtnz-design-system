{{! DEVELOPER NOTE: This template uses triple-bracket "{{{"
    which disables HTML escaping.
    Please ensure these variables are properly escaped:
     - children.
    The reason for this is to allow raw HTML, for values such as (eg) '<span lang="mi">Māori</span>'. }}

<nav
aria-label="Main"
class="g-main-nav{{#isOpen=true}} g-main-nav--open{{/isOpen=true}}{{#isOpen=false}} g-main-nav--closed{{/isOpen=false}}"
{{#menuContainer}}
  id="{{menuContainer}}"
{{/menuContainer}}
role="navigation"
>
  
  
<ul
class="g-main-nav__ul"
>
  
    
  {{{children}}}
  
  
</ul>
  

</nav>