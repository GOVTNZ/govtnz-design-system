{{! DEVELOPER NOTE: This template uses triple-bracket "{{{"
    which disables HTML escaping.
    Please ensure these variables are properly escaped:
     - label,
     - hint.
    The reason for this is to allow raw HTML, for values such as (eg) '<span lang="mi">Māori</span>'. }}

<div
class="g-radios__item"
>
  
  
<input
{{#hintId}}
  aria-describedby="{{hintId}}"
{{/hintId}}
class="g-radios__input"
{{#radioId}}
  id="{{radioId}}"
{{/radioId}}
{{#gov}}
  name="{{gov}}"
{{/gov}}
type="radio"
{{#disabled}}
  disabled="{{#disabled}}disabled{{/disabled}}"
{{/disabled}}
{{#readOnly}}
  readonly="{{#readOnly}}readOnly{{/readOnly}}"
{{/readOnly}}
{{#autoFocus}}
  autofocus="{{#autoFocus}}autoFocus{{/autoFocus}}"
{{/autoFocus}}
{{#value}}
  value="{{value}}"
{{/value}}
{{#checked}}
  checked="{{#checked}}checked{{/checked}}"
{{/checked}}
/>
  
  
<label
class="g-radioBlock-label g-radios__label"
{{#radioId}}
  for="{{radioId}}"
{{/radioId}}
>
  
    
  {{{label}}}
  
  
</label>
  
  
  
    
<div
class="g-radioBlock-hint g-checkboxes__hint"
{{#hintId}}
  id="{{hintId}}"
{{/hintId}}
>
  
      
  {{{hint}}}
  
    
</div>
  
  
  

</div>