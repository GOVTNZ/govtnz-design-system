import * as React from "react";

type Props = {};

const ErrorMessage = ({  }: Props) => (
  <span className="g-error-message">
    Error message about full name goes here
  </span>
);
ErrorMessage.props = [];
export default ErrorMessage;
